info:
  name: endpoint-stats
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/stats/{{endpointId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
