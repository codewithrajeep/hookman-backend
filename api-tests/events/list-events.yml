info:
  name: list-events
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/events"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
