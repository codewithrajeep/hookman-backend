info:
  name: get-dead-letter
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/delivery/dead-letters/{{eventId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
