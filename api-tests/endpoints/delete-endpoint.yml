info:
  name: delete-endpoint
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/endpoints/{{endpointId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
